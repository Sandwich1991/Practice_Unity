using UnityEngine;
public class Define
{
    public enum MouseEvent
    {
        Click,
        Press,
    }
    
    public enum EntityState
    {
        Idle,
        Move,
    }
    
    public enum Scene
    {
        Unknown,
        CharacterSelect,
    }
}
